{
  "framework": "nextjs",
  "buildCommand": "pnpm build || npm run build",
  "devCommand": "pnpm dev || npm run dev",
  "installCommand": "pnpm install || npm install",
  "outputDirectory": ".next",
  "regions": ["iad1"]
}

